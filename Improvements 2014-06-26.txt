FrameViewer changes:
Marker types: Change from selection box to radio buttons √

Marker keyboard shortcuts as specified in document attached to E-mail message
Shortcut -> 1-step set up marker with no dialog

For marker interactions, add a field for the ID of the ant that was met √

Fix issue when the ant ID in the marker dialog is not always recalled

Fix issue of files on Windows not being saved as .csv

Right-click to create interaction marker, left-click to create non-interaction marker √

Long-term:
Define regions, then when a marker is placed its region is filled in automatically
(Maybe use a low-resolution bitmap)
